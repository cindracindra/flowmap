[project]
name = "flowmap_java"
version = "0.1.0"
description = ""
authors = [
    {name = "cindra",email = "cc4625@ic.ac.uk"}
]
requires-python = ">=3.12,!=3.14.1"
dependencies = [
    "cpgqls-client (>=0.0.9,<0.0.10)",
    "networkx (>=3.6.1,<4.0.0)",
    "plotly (>=6.9.0,<7.0.0)",
    "nltk (>=3.10.0,<4.0.0)",
    "wordninja (>=2.0.0,<3.0.0)",
    "scikit-learn (>=1.9.0,<2.0.0)",
    "sentence-transformers (>=5.6.1,<6.0.0)",
    "groq (>=1.6.0,<2.0.0)",
    "dotenv (>=0.9.9,<0.10.0)",
    "numpy (>=2.5.1,<3.0.0)"
]


[tool.poetry]
packages = [{ include = "processor" }]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
